event_inherited();  //Mit diesem Befehl wird zugelassen das sowohl der Code vom 
					// übergeordneten Element (hoer o_ship_parent) als auch der
					// eigene Code des Events ausgeführt wird

bounce_off_screen_edge();